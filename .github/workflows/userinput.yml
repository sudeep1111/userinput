name: Convert Input to Capital

on:
  workflow_dispatch:
    inputs:
      user_input:
        description: 'Enter a string to convert to capital letter'
        required: true

jobs:
  create-pr:
    name: "Target PR to Main Branch"
    uses: sudeep1111/userinput/.github/workflows/create-pr.yaml@DEV
    with:
      github_reviewers: "naveen184"
      base_branch: DEV
      head_branch: deploy
    secrets: inherit
  capitalize:
    runs-on: ubuntu-latest
    steps:
      - name: Convert Input to Capital
        run: echo "${{ github.event.inputs.user_input }}" | tr '[:lower:]' '[:upper:]'
